export type PRODUCT_COLOR = {
  label: string;
  value: string;
  tw: string;
};

export type PRODUCT_TYPE = {
  label: string;
  value: string;
  price: number;
};

export type PRODUCT_SIZE = {
  label: string;
  value: string;
};
